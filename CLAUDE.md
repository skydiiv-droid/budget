# 가계부

아이폰으로 쓰는 개인 가계부. 카드·은행 문자를 단축어가 Firebase 로 넘기면
알아서 분류하고, 웹앱에서 본다.

## 정해진 것 — 다시 묻지 말 것

### 환경
- **아이폰.** 다른 앱 알림은 못 읽는다(iOS 에 API 가 없다). 그래서 **문자 + 단축어**가
  유일한 길이다. 이 결론은 이미 났다.
- **위젯은 전부 Scriptable 로 만든다.** 이 사람이 쓰는 위젯은 다 Scriptable 이다.
  웹앱은 진짜 위젯을 못 만드니 읽기 전용 함수를 두고 Scriptable 이 불러다 그린다.
- 맥은 없다. 개발은 병원 PC 에서, 서버로 계속 켜 둘 수는 없다.
- 배포는 GitHub Actions → Firebase Hosting / Firestore / Functions.

### 사람
- 종합병원 간호사, 2024년 입사. **3교대.** 여러 진료과를 돈다.
- **급여일 5일**, **현대카드 결제일 10일.**
- 카드 둘을 따로 잡는다 — 현대 이마트Plus, 현대 미래에셋.
- 주거래 우리은행. 입출금 문자는 잔액을, 카드 문자는 사용금액과 누적을 찍는다.
- 목표: 두 달 안에 리볼빙 + 마이너스통장 정리 → 그 뒤로는 저축 습관.

### 돈 계산
- **고정지출은 내역을 만들지 않는다.** 그것도 결국 문자로 들어오므로, 등록한 항목이
  하는 일은 **들어온 문자 중에 그것이 있는지 확인하는 것**이다. 적어 넣으면 두 번 세어진다.
  문자에 찍히는 말이 이름과 다르면 키워드로, 다른 데서 긁힌 걸 가르려면 결제 수단으로 찾는다.
- **고정지출이 안 들어왔는지 보는 기한**: 예정일이 평일이면 그 다음 날까지,
  쉬는 날이면 **다음 영업일의 그 다음 날**까지. 휴일에 걸린 자동이체는 다음 영업일에 빠진다.
- **알림에는 끝이 있어야 한다.** 묻기만 하고 치울 길이 없으면 알림은 영영 남고,
  남아 있는 알림은 곧 안 보는 알림이 된다. 안 들어온 고정지출의 답은 셋이다 —
  해지했다(`active:false`) · 이 결제건이다(거래에 `recurringId`) · 이번 달은 넘긴다(`skipMonths`).
  "이 결제건이다"를 고르면 그 가맹점명을 키워드로 배워 다음 달엔 저절로 잡는다.
- **카드사 누적에는 리볼빙 이월잔액이 얹혀 있다.** 이달 거래만 세어 견주면 이월분이
  통째로 "문자를 놓친 결제"로 뜬다. 다만 안 얹는 카드사도 있을 수 있으니, 빼서 오히려
  더 벌어지면 손대지 않는다.
- **카드는 계좌다.** 카드값 납부는 `transfer` 라서 지출로 두 번 세지 않는다.
- **현대카드 청구 기간은 달력 월이다.** 10월 10일에 내는 건 9월 1일~30일 쓴 것.
  기간 경계는 달력 날짜에 두고 **빠지는 날만** 영업일로 민다.
- **현대카드는 카드가 둘이어도 누적을 합쳐 찍는다.** 그래서 카드를 묶어
  (`statementGroupId`) 청구서 한 장으로 세고, 누적 대조도 묶음 단위로 한다.
- **리볼빙은 대출이 아니라 카드의 성질이다.** 약정비율만큼 내고 나머지가
  이자와 함께 다음 달로 넘어간다. 이월잔액은 카드사가 정하는 숫자라
  앱이 셀 수 없다 — 사용자가 카드 앱에서 보고 넣는다.
- **마통이라고 늘 마이너스인 건 아니다.** 플러스일 때 받는 이자(`ratePlus`)를 따로 둔다.
- 연 1회 고정비는 **열두 달로 나눠** 여력에 넣는다. 나가는 달에만 세면
  나머지 열한 달은 돈이 있는 줄 안다.
- **계획과 실제를 섞지 않는다.** 섞으면 구독이 두 번 세어진다.
- **마이너스통장은 입출금 계좌이자 빚이다.** 잔액이 음수면 그 절댓값이 빚이다.
- 카드 미결제액은 빚으로 세지 않는다. 리볼빙으로 넘어간 것만 빚이다.
- 갚을 여력이 0 이하면 "몇 달 걸린다"를 말하지 않는다. 무한대는 답이 아니다.
- **지난달 대비는 같은 날짜까지만 견준다.** 달 전체와 견주면 달 초엔 늘 덜 쓴 게 된다.

### 근무표 앱
- 근무표는 **따로 만든 앱**(Firebase 실시간 데이터베이스)에 이미 있다. 가계부는 **읽기만** 한다.
- **실제 구조는 `duties/{YYYY-MM}/{사번}` 이고 날짜마다 `{ shift:"D", team:"A" }` 가 들어 있다.**
  `shift` 만 1일부터 이어 붙이면 `DENNOO` 가 된다. 서버는 이어 붙여 주지 않는다 — 우리가 한다.
- **그래도 저장 구조를 못 박지 않는다.** 바뀔 수도 있어서, 나무를 훑어 듀티처럼 생긴 값을
  찾고 그 위 키에서 사람과 달을 읽는다. 한 줄로 적힌 것도, 날짜별 한 글자도, 객체로 싼 것도 받는다.
  `shifts` 같은 그릇 이름은 사람으로 치지 않고, 아이디로 갈라 둔 경우 안에 적힌 `name` 을 쓴다.
- **사번도 날짜도 숫자다.** 1~31 은 날짜, 그보다 긴 숫자는 사람으로 가른다.
- 맨 위 주소를 넣으면 데이터베이스 전체를 받는다. `…/duties/2026-09` 처럼 좁혀 넣으면
  받은 값에 달이 없으므로 **주소에서 달을 읽어** 넘긴다.
- 여러 사람 것이 한 곳에 있다. 누구 것인지 골라 `dutyPerson` 에 기억해 둔다.
- 글자 수가 그 달 날수와 안 맞으면 **넣되 어긋났다고 말한다.** 하루만 밀려도 근무별 지출이
  통째로 틀리는데, 틀린 줄 모르는 게 제일 나쁘다.

### 보안
- 사람은 구글 로그인, 기계(단축어)는 토큰. 토큰으로는 **넣기만** 된다.
- 토큰을 묻거나 되읊지 않는다. 주소 줄이는 서비스는 쓰지 않는다 — 주소+토큰이면 쓰기가 된다.
- 위치를 거래마다 저장한다. 잔고보다 민감하다는 걸 알고 쓰기로 했다.
- 근무표 앱이 로그인 없이 읽힌다는 건 주소를 아는 누구나 근무표를 본다는 뜻이다.
  본인에게 말했고, 그대로 두기로 했다. 다시 꺼내지 않는다.

### 말투
- **화면의 말은 공적인 문어체로 쓴다.** 구어체 종결(-어요 · -예요 · -네요)을 쓰지 않고
  `-합니다` · `-됩니다` 또는 명사로 끝낸다. 한때 "쓰는 사람 말로" 규칙이 있었으나
  읽기 어렵다고 해서 뒤집었다. 지금 쓰는 말:
  가맹점 · 카테고리 · 지출 · 수입 · 이체 · 자산 · 부채 · 등록 · 수정 · 삭제.
  (쓰지 않는 말: 가게 · 갈래 · 쓴 돈 · 옮김 · 빚 · 넣기 · 고치기)
- **군더더기 문구는 지운다.** 읽어도 무엇을 다르게 하지 않는 설명은 없는 편이 낫다.
- 코드 주석은 예외다. 그건 나중에 읽을 나에게 하는 말이라 지금대로 둔다.
- 커밋 메시지는 무엇을 왜 고쳤는지 한국어로. 모델 이름은 어디에도 쓰지 않는다.

## 돌려볼 것
```
node --test firebase/shared/*.test.js      # 순수 로직
```
화면은 헤드리스 크로미움으로 실제로 띄워서 본다. Firebase 모듈을 스텁으로
갈아끼우고 가짜 데이터를 넣는다 (지난 세션의 `prev/` 하네스 참고).

## 해 둔 것
1. 계좌·카드 모델 — 통합 입력, 신용/체크/하이브리드, 마이너스통장(부호 대신 체크),
   결산 기준일, 같은 기간 비교, 카드값 예상, 영업일(나가는 돈은 밀리고 들어오는 돈은 당겨짐)
2. 빠른 수동 추가(+), 승인취소 상계, 홈 재편(가리기 모드 · 목표 일반화 · 순서)
3. 검색(부분 일치), 갈래별 예산, 달마다 쓴 돈 그래프, 태그, 메모, CSV
4. 고정비 자동 탐지(제안만), 근무별 지출, Scriptable 위젯 + summary 함수, 알림
5. 고정지출 확인(`shared/fixed.js`) — 결제 수단 · 문자 키워드 · 금액 변동 · 미출금 알림
6. 근무표 가져오기(`shared/duty.js`) — 따로 만든 근무표 앱에서 듀티를 읽어 온다

## 토큰이 둘이다
- `config/ingest.token` — 단축어가 문자를 **넣는** 데만 쓴다
- `config/ingest.widgetToken` — 위젯이 **합계만 읽는** 데 쓴다.
  넣는 쪽이 읽기까지 되면 "넣기만 된다"가 거짓말이 된다.

## 화면 규칙
- **돈의 부호는 국내 관례다 — 플러스가 빨강(`--up`), 마이너스가 파랑(`--down`).**
  들어온 돈 · 잔고는 빨강, 나간 돈 · 빚은 파랑.
  "예산을 넘겼다"는 부호가 아니라 경고라서 호박색(`--warn-mark`)을 쓴다.
- **한 폼 안에서 `name` 을 겹치지 않는다.** 겹치면 숨은 쪽 값이 잡힌다.
  두 번 겪었다. `forms.test.js` 가 잡는다.
- 금액을 찍는 자리에는 가릴 class(`num`·`big`·`tx-amt`…)를 붙인다.
  안 붙이면 그 숫자만 맨눈에 남는다. `mask.test.js` 가 잡는다.
- 색 이름은 `:root` 에 정의된 것만 쓴다. `style.test.js` 가 잡는다.
- 아이폰 숫자 키패드에는 **빼기 기호가 없다.** 부호를 치게 하지 말고 물어본다.
- **설정 문서에 쓸 때는 `{ merge: true }` 를 빠뜨리지 않는다.** 한 폼이 제 칸만 담아
  통째로 쓰면 다른 데서 넣어 둔 설정이 조용히 날아간다. 한 번 겪었다 —
  수입·예산을 저장하면 갈래별 예산과 "다시 묻지 않을 목록"이 사라졌다.
- **글자를 치는 칸이 든 자리를 다시 그리지 않는다.** 입력칸이 새것으로 갈리면
  한글 조합이 끊겨 글자가 깨진다. 검색은 칸 아래만, 규칙 거르기는 줄을 숨기는 것만으로 한다.
- **위젯 색은 두 벌을 다 들고 다닌다.** 위젯 바탕은 시스템 테마를 따라가므로
  한쪽만 박으면 반대 테마에서 안 보인다. `dyn(밝은, 어두운)` 을 거친다.
  `widget.test.js` 가 잡고, `node widget/preview.mjs` 로 두 테마를 눈으로 본다.
  (웹앱은 제 바탕을 깔아 늘 밝은 모드라 이 문제가 없다.)

## 아이디어로만 (아직 안 함)
- 결제 직후 알림 — 단축어 응답에 "오늘 남은 돈"을 실어 알림으로 띄우기
- 은행 잔고 자동 대조 — 문자에 찍힌 잔액이 진실. 계좌별 앵커, 카드 계정은 제외
